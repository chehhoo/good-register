# API base URL — points to the good-api backend
# Production: https://api.goodvessel.org/api
# Local dev:  (leave unset — defaults to /api via nginx proxy)
VITE_API_BASE_URL=https://api.goodvessel.org/api
